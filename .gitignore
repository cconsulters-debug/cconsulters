# Abhängigkeiten
node_modules/

# Lokale Umgebungsvariablen (Secrets NIEMALS committen)
.env
.env.local

# Netlify-Lokalzustand
.netlify/

# Build-Output (wird bei jedem Netlify-Build aus /scripts generiert)
gebiete/*.html
sitemap.xml

# Mein Rechtshelfer: site/ entsteht aus app/ und recht/ via scripts/build_site.py
mein-anwalt/site/

# Betriebssystem
.DS_Store
